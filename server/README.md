## About this app

This Laravel based application is backend part of full stack application. It exposes REST API with book list where user can login, do CRUD operations with books he created, search books, add/remove from favorites list.\
\
After installation, run migrations and seed the database. Seeding will create a user and add books. See database seeders file to find user login and password.